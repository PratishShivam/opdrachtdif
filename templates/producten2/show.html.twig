{% extends 'base.html.twig' %}

{% block title %}Producten2{% endblock %}

{% block body %}
    <h1>Producten2</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ producten2.id }}</td>
            </tr>
            <tr>
                <th>Productnaam</th>
                <td>{{ producten2.Productnaam }}</td>
            </tr>
            <tr>
                <th>Prices</th>
                <td>{{ producten2.Prices }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('producten2_index') }}">back to list</a>

    <a href="{{ path('producten2_edit', {'id': producten2.id}) }}">edit</a>

    {{ include('producten2/_delete_form.html.twig') }}
{% endblock %}
